Spring Boot micro service needs to expose these endpoints 
a. API to generate the shortened URL for any long URL
b. API  to fetch all the submitted requests 
(Use an in memory database to stored logs of all submitted requests)
c. API to clear the logs 
